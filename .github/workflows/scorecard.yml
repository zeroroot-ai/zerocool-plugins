# SPDX-License-Identifier: Apache-2.0
#
# OSSF Scorecard supply-chain health analysis.
#
# The action requires the repository to be public and certain
# permissions to be granted at the org level. Results are uploaded as
# SARIF to the Security tab; the badge URL after the first run will be
#
#   https://api.scorecard.dev/projects/github.com/zeroroot-ai/setec/badge
#
# and can be placed in README.md.
name: OSSF Scorecard

on:
  branch_protection_rule:
  push:
    branches: [main]
  schedule:
    # Weekly run: Monday 07:00 UTC.
    - cron: "0 7 * * 1"

permissions: read-all

concurrency:
  group: scorecard-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analysis:
    name: Scorecard analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      id-token: write
      contents: read
      actions: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Run analysis
        uses: ossf/scorecard-action@2d1146689b8cda280b9bc96326124645441f03bc # v2.4.4
        with:
          results_file: results.sarif
          results_format: sarif
          # Publishing needs a public repo; private repos analyse locally only.
          publish_results: ${{ !github.event.repository.private }}

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: SARIF file
          path: results.sarif
          retention-days: 5

      - name: Upload to code-scanning
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938 # v4
        with:
          sarif_file: results.sarif
