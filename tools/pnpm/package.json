{
  "name": "zerocool-build-tools",
  "private": true,
  "description": "pnpm for the image build stages, hash pinned. Node 25 and later ship no corepack, so the Dockerfiles install pnpm with npm ci from this lockfile. Keep the version equal to packageManager in the root package.json, so pnpm never self-switches at build time.",
  "dependencies": {
    "pnpm": "10.34.5"
  }
}
