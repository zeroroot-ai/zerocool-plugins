{
  "name": "@zerocool/gibson-client",
  "version": "0.0.0",
  "description": "TypeScript client for the Gibson daemon: connect-es bindings + Capability Grant auth.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "buf generate buf.build/zeroroot-ai-platform/sdk && buf generate buf.build/bufbuild/protovalidate",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "@connectrpc/connect": "^2.0.0",
    "@connectrpc/connect-node": "^2.0.0"
  }
}
